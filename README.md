# Abelian Sandpile Simulator

C++ implementation of a sandpile model with configurable grid size and initial state.

The simulator performs stabilization and serializes the resulting state as a binary BMP image.
